[phases.setup]
nixPkgs = ["python311", "gcc", "postgresql", "pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements-railway.txt"
]

[start]
cmd = "python main.py" 